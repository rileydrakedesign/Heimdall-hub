# Memory Flush — 2026-02-19 ~02:03–09:18 UTC

- Built asset-cleaning pipeline in `work/riley-portfolio`: `scripts/clean-assets.mjs` removes backgrounds (white/teal) + crops to content bounds using pngjs.
- Installed ImageMagick on VPS to convert incoming JPG → PNG for further processing.
- Created/iterated extraction scripts: `scripts/extract-positions.mjs` (initial) and `scripts/extract-positions-v2.mjs` (multi-scale template matching against cleaned reference; fixed scoring to compare normalized match%).
- Identified root issue: inconsistent canvas sizes/scales across base plate (1024×1024), assets (mixed), and reference composite; also some assets had solid white backgrounds.
- Generated cleaned transparent assets in `public/iso-pixel/dev-room/cleaned/` and updated `manifest.json` to point to cleaned assets.
- Established canonical positioning reference: `public/iso-pixel/dev-room/cleaned/reference_scene.png` (sparkle removed, cropped). Saved and pushed to repo.
- Updated RoomCanvas editor overlay to use the canonical reference (`/iso-pixel/dev-room/cleaned/reference_scene.png`) and fixed lint issue (prefer-const for dragOffset). Pushed.
- Moved portfolio documentation into the project repo under `work/riley-portfolio/docs/` and trashed workspace-level duplicates.
- Resolved a git rebase conflict on manifest.json by keeping the remote’s manually-tuned `scale`/positions.
- User preference/decision: Riley wants eventual sellable portfolio templates; recommendation is Option B (grid/data-driven) or hybrid grid-first with composed skin.
- OPS daily alert persists: ContentAutomationPlatform repo wiring drift detected (0 critical, 12 high); sent Telegram alert to Riley.
