- Updated OpenClaw routing: set `agents.defaults.model.primary` to **anthropic/claude-sonnet-4-5** (alias: `sonnet`) for cheaper day-to-day use.
- Kept **anthropic/claude-opus-4-6** (alias: `opus`) as the **first fallback** for escalation/critical tasks; remaining fallbacks unchanged.
- Gateway restart triggered by `config.patch`; restart completed successfully (config-apply: ok).
- User preference clarified: default to smaller/cheaper model for general tasks; explicitly use Opus 4.6 only for important/complex work (e.g., large coding additions).
