- Config change: switched Anthropic authentication from API key to OAuth.
  - `auth.profiles["anthropic:default"].mode` set to `oauth`.
  - `models.providers.anthropic.auth` set to `oauth`.
- Gateway applied config patch and triggered a restart (SIGUSR1) to activate the new Anthropic OAuth settings.
