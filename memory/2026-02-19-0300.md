# Memory Flush — 2026-02-19 03:00 UTC (pre-compaction)

## Portfolio repo consolidation
- All portfolio docs moved into `work/riley-portfolio/docs/` (IMPLEMENTATION_PLAN.md, PLAN_ORIGINAL.md, PROJECT_NOTES.md). Old copies trashed. AGENTS.md router updated.
- Asset cleaning pipeline built: `scripts/clean-assets.mjs` removes backgrounds + crops to content bounds. `scripts/extract-positions.mjs` does template-matching coordinate extraction.
- Cleaned transparent PNGs saved to `public/iso-pixel/dev-room/cleaned/`.
- True reference scene: `cleaned/reference_scene.png` (662×585) — Riley manually removed a sparkle artifact, then we cropped. This is the positioning ground truth.

## Key asset findings
- Assets are at inconsistent scales: base plate 1024×1024, reference 1280×626 (now 662×585 cleaned), character/cabinet 1024×1024, desk 581×571.
- Character and cabinet have white backgrounds (now removed). Base plate has dark teal background (now removed).
- Remote repo already had manual positioning with `scale` properties in manifest.json (desk 0.28x, character 0.14x, cabinet 0.2x). Kept those over auto-extracted values.
- Assets need scaling down to match reference scene proportions before positioning.

## Infrastructure
- ImageMagick installed on VPS (`sudo apt-get install imagemagick`) for JPG→PNG conversion.
- `pngjs` added as devDependency in riley-portfolio for image processing scripts.
- npm node_modules had stale rename artifacts causing ENOTEMPTY errors — cleaned with `find node_modules -name ".*" -type d -exec rm -rf`.

## Nightly validation pipeline
- First run fired at 03:00 UTC. Picked up 3 new ideas (scene-splitter, polymarket-sentiment, dev-infra-agent). Spawned parallel sub-agents. Reports pending.
