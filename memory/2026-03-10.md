- Morning brief system advanced from spec-only to runnable pipeline: added generator script `web/scripts/generate-brief.mjs` and a Hub UI page `/briefs` to browse runs.
- New config artifact: `data/briefs.yaml` is the formal “brief attunement” source-of-truth (profiles, sections, sources); default profile `default-morning` with sections: Top 5, Current Events, Markets, AI/Dev, Ops, Local(optional).
- New tool/data integration (static-first): Markets quotes via **Stooq CSV** (https://stooq.com) added to generator; arXiv via RSS (export.arxiv.org) already used; RSS parsing is minimal and configurable.
- Generator output contract implemented: writes markdown brief to `projects/daily-debrief/runs/YYYY-MM-DD.md` (sample generated for 2026-03-10).
- Hub UI change: dashboard now links to `/briefs` (alongside `/projects` and `/tasks`).
- Bugfix: `githubFileUrl` import corrected to come from `web/src/lib/runs.ts` (not `repo.ts`) to make `/briefs` build.
- User preference/requirement reiterated: morning brief should include (a) general current events, (b) tailored markets brief for watchlist + movers + why, (c) targeted AI/dev news + top papers; Hub must allow tuning sources/format/output.
- Pending input from user (to tune config): trusted news RSS feeds, market watchlist tickers/segments, AI scope (categories/keywords/authors).
