- Heimdall Hub stack confirmed: static Next.js dashboard in `web/` (Next.js 16 App Router) that loads YAML at build time; projects source-of-truth is `data/projects.yaml`.
- User preference: task tracker UX should be hybrid Kanban+list, Asana-like; list grouped into global categories: backlog / in progress / blocked / done.
- User decision: agents are allowed to see personal tasks to enable a reminder loop (no privacy restriction requested).
- User decision: board columns should be global but also customizable per project.
- Implementation change: added new durable tasks store `data/tasks.yaml` (YAML) to mirror/build on TASKS.md-style tracking.
- Implementation change: added task loader `web/src/lib/tasks.ts` to read `data/tasks.yaml`.
- Implementation change: added new Next.js route/page `web/src/app/tasks/page.tsx` to render tasks grouped by status buckets.
- Implementation change: updated dashboard home (`web/src/app/page.tsx`) to include a "View tasks" link.
- Operational note: confirmed `TASKS.md` not present (only `.trash-TASKS.md`); current task queue elsewhere (HEARTBEAT.md).
- Build/ops state: `web` Next build succeeded after changes; noted Turbopack warning about multiple lockfiles (root vs `web/package-lock.json`).
- Workflow direction: marketing/PM tracking should attach to each project once it reaches an "active" phase; KPIs can be defined later; start with workflow scaffolding first.
