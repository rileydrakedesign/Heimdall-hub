- Clarified taxonomy for Riley: skills = folders under `/home/claw/clawd/skills` with `SKILL.md`; autonomous pipelines/jobs = cron-scheduled agentTurns.
- Confirmed idea validation is a cron pipeline (not a packaged skill): nightly 03:00 UTC, reads `data/projects.yaml` `status: idea`, writes reports to `projects/program-manager/research/idea-validation/<id>.md`, exits early if no new ideas.
- Inventoried autonomous cron jobs (5): Idea validation nightly; OPS daily repo health; PM research sweep daily; PM brief M/W/F; OPS weekly VPS snapshot. Noted OPS daily + PM research sweep currently show “cron delivery target is missing” errors.
- User preference for diagram cards: text must be readable on-phone; avoid tiny horizontal flows; prefer vertical layout/stems and non-plain styling.
- Used `diagram-thread-cards` deterministic Mermaid→PNG renderer to produce Thread 1 diagrams for 4 checkup skills; iterated style/layout to improve readability and prevent cutoffs.
- Generated/updated artifacts:
  - `output/thread-diagrams/checkup-skills/01_vps-security-audit.(mmd|png)` (vertical, styled)
  - `output/thread-diagrams/checkup-skills/02_prod-wiring-audit.(mmd|png)` (simplified + re-centered to avoid bottom cutoff)
  - `output/thread-diagrams/checkup-skills/03_supabase-schema-guardian.(mmd|png)` (simplified + re-centered to avoid bottom cutoff)
  - `output/thread-diagrams/checkup-skills/04_prompt-safety-style-qa.(mmd|png)` (vertical, styled)
- Delivered updated screenshots to Riley via Telegram.
- No OpenClaw config changes, gateway restarts, or updates performed.
- No new external integrations added; only local file generation + Playwright-based rendering.
- Safety/permissions: no public posting; continued ask-first stance for external actions.
