# Memory Flush — 2026-02-20 ~18:24 UTC

## Portfolio — layout refactor (centering, no containers)
- Riley requested the isometric room scene be the ONLY element on the page, centered dead-center, no bounding containers, scale unchanged (0.75).
- Changes made to `work/riley-portfolio/`:
  - `src/app/page.tsx`: removed `<main>` wrapper; page now renders bare `<Suspense><RoomCanvas /></Suspense>`.
  - `src/components/RoomCanvas.tsx`: host div changed from `className="h-full w-full"` to `style={{ position: "fixed", inset: 0, width: "100vw", height: "100vh" }}`.
  - `src/app/globals.css`: removed dark-mode media query; hardcoded dark background (#0a0a0a); added `margin:0; padding:0; overflow:hidden` on html,body.
- Changes NOT committed yet — waiting on Riley's visual confirmation.

## VPS infra constraints (important for future work)
- VPS has only 2GB RAM, no swap. Next.js dev server (Turbopack) + Playwright headless Chromium cannot run simultaneously — OOM kills one or both.
- Next.js 16.1.6 Turbopack first-compile of `/` takes ~25-30s on this VPS.
- `next build` with `output: "export"` works (static export) but was reverted since it was only used for screenshot attempt.
- Workaround attempted: Python `http.server` serving `out/` dir + existing Chrome CDP (port 9222) — CDP connect timed out (30s).
- Existing Chrome on port 9222 is a Reddit-profile Playwright Chromium instance running since Feb 6.
- `browser` tool with `target=host` / `profile=openclaw` returned "No supported browser found" error.
- **Conclusion**: screenshots on this VPS require either (a) killing the existing Chrome first to free RAM, or (b) Riley running locally.

## Operational notes
- `next.config.ts` was temporarily set to `output: "export"` then reverted back to default.
- Static build output left in `work/riley-portfolio/out/` (can be cleaned up).
