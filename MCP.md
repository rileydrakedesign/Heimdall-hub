# Playwright MCP

This repo has Playwright + @playwright/mcp installed.

Typical usage (when wiring into an MCP client):

- Command: `npx @playwright/mcp`
- Transport: stdio

Local sanity check:

```bash
npx @playwright/mcp --help
```
